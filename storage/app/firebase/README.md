# Firebase Admin credentials (local / server only)

Keep this folder on the server. Put your key here:

```text
storage/app/firebase/service-account.json
```

This JSON file is gitignored and will NOT be deleted by normal git pull.
Never commit `service-account.json`.
